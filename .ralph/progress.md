- item worked on: packaging — added `pyproject.toml`, packaged `parakeet` entry point, and moved dictation runtime into `src/parakeet/` with `transcriber.py` as a compatibility wrapper into `parakeet dictation`.
  key decisions: keep milestone-1 scope to packaging only; preserve the existing manual start/stop lifecycle; make top-level `parakeet` a subcommand CLI while keeping legacy top-level dictation flags and `transcriber.py` behavior working.
  files changed: `pyproject.toml`, `src/parakeet/__init__.py`, `src/parakeet/cli.py`, `src/parakeet/dictation.py`, `transcriber.py`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/parakeet --help` ✅; `./.venv/bin/parakeet dictation --help` ✅; `./.venv/bin/parakeet --list-devices` ✅ (enumerated `pulse` and `default` input devices, with ALSA/JACK stderr noise from host audio probing); `./.venv/bin/python transcriber.py --help` ✅.
  notes for next fresh-context iteration: highest-priority remaining work is the architecture/contracts spine (`src/parakeet/types.py` and `src/parakeet/errors.py`) so later diagnostics, devices, benchmark, and tests can share stable internal models and error codes.
- item worked on: architecture — added shared contract modules `src/parakeet/types.py` and `src/parakeet/errors.py`, then refactored dictation flow to use `DictationConfig`, `AudioDevice`, `TranscriptionResult`, and application error codes/exceptions instead of ad hoc inline structures.
  key decisions: keep this iteration strictly on the architecture/contracts spine; model doctor/benchmark/device schemas as stable internal dataclasses/protocols now, while leaving functional command implementations for later items; preserve the existing explicit-start/manual-stop lifecycle unchanged.
  files changed: `src/parakeet/types.py`, `src/parakeet/errors.py`, `src/parakeet/dictation.py`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -c "from parakeet.errors import AppError, ExitCode, AUDIO_BACKEND_UNREACHABLE; from parakeet.types import AudioDevice, DictationConfig, DoctorReport, BenchmarkReport, TranscriptionResult, VadBackend, TranscriptionEngine; print('contracts-ok')"` ✅; `./.venv/bin/parakeet --help` ✅; `./.venv/bin/parakeet dictation --help` ✅; `./.venv/bin/python transcriber.py --help` ✅.
  notes for next fresh-context iteration: highest-priority remaining item is configuration precedence, because `DictationConfig` now exists as the stable target and later doctor/devices/benchmark work will benefit from deterministic runtime config resolution.
- item worked on: configuration — added `src/parakeet/config.py`, wired dictation CLI/config parsing through deterministic precedence resolution, and introduced the first automated pytest coverage for defaults, config-file values, env overrides, and CLI overrides.
  key decisions: treat configuration as the highest-priority remaining dependency because diagnostics, devices, benchmark, and VAD all need one shared runtime config source; keep scope to parsing/resolution only, without pulling output, doctor, or benchmark behavior into this iteration.
  files changed: `src/parakeet/config.py`, `src/parakeet/cli.py`, `src/parakeet/dictation.py`, `tests/test_config.py`, `pyproject.toml`, `requirements.txt`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`4 passed`); `./.venv/bin/parakeet dictation --help` ✅.
  notes for next fresh-context iteration: the highest-priority remaining item is diagnostics (`parakeet doctor` / `parakeet devices`) because the packaging spine, shared contracts, and configuration precedence are now in place for stable CLI and JSON contract work.
- item worked on: functional (`parakeet devices --json`) — added a dedicated `devices` subcommand plus `src/parakeet/audio.py` device enumeration so the packaged CLI now returns the spec JSON schema with stable device ordering and an empty-array zero-device contract.
  key decisions: keep this iteration strictly to the devices contract as the foundation for later doctor work; make enumeration lazy and lightweight so `devices` does not pull in model/runtime dependencies; preserve the user-controlled dictation lifecycle unchanged.
  files changed: `src/parakeet/audio.py`, `src/parakeet/cli.py`, `src/parakeet/dictation.py`, `tests/test_devices.py`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`7 passed`); `./.venv/bin/parakeet devices --json` ✅ (returned schema_version 1 with sorted `pulse`/`default` devices; ALSA/JACK probe noise appeared on stderr from host audio enumeration but exit code remained 0).
  notes for next fresh-context iteration: highest-priority remaining work is `parakeet doctor`, because devices enumeration and shared config/contracts are now in place for deterministic readiness classification and exit-code mapping.
- item worked on: diagnostics (`parakeet doctor`) — added `src/parakeet/doctor.py`, wired a `doctor` subcommand into the packaged CLI, and implemented deterministic WSL/Pulse/audio-device/clipboard/CUDA reporting with ok/warn/fail exit-code mapping while keeping default mode side-effect free for model checks.
  key decisions: keep this iteration strictly to default doctor behavior and leave `--check-model-cache` for the next diagnostics item; treat clipboard readiness as copy-backend availability so WSL `clip.exe` counts as usable without forcing model work or downloads; preserve the explicit user-controlled dictation lifecycle unchanged.
  files changed: `src/parakeet/audio.py`, `src/parakeet/cli.py`, `src/parakeet/doctor.py`, `tests/test_doctor.py`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pytest -q` ✅ (`11 passed`); `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/parakeet doctor --json` ✅ (returned schema_version 1 with WSL/Pulse/audio/clipboard/CUDA/model status and exit code 0 on this host; ALSA/JACK probe noise still appears on stderr during device enumeration).
  notes for next fresh-context iteration: highest-priority remaining work is `parakeet doctor --check-model-cache`, because the default diagnostics contract and exit-code spine now exist and the next risk is deterministic local model-cache/import readiness without downloads.
- item worked on: diagnostics (`parakeet doctor --check-model-cache`) — added `src/parakeet/model.py`, wired the doctor CLI flag, and implemented deterministic local Hugging Face cache/import readiness reporting with fail-state issue mapping while avoiding model load or download.
  key decisions: prioritize this item because it sits on the diagnostics critical path for later benchmark/offline-readiness work with minimal dependency fan-out; treat missing local cache or broken model imports as explicit fail issues for the opt-in cache-readiness check; keep default `parakeet doctor` side-effect free by skipping model cache checks unless the flag is passed.
  files changed: `src/parakeet/cli.py`, `src/parakeet/doctor.py`, `src/parakeet/model.py`, `tests/test_doctor.py`, `tests/test_model.py`, `.ralph/items.json`.
  verification commands run and outcomes: `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pytest -q` ✅ (`17 passed`); `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/parakeet doctor --json` ✅ (schema_version 1, exit code 0; ALSA/JACK probe noise still appears on stderr during host audio enumeration); `./.venv/bin/parakeet doctor --check-model-cache --json` ✅ (reported local cache path plus `import_ready=true`, exit code 0, no model load/download).
  notes for next fresh-context iteration: highest-priority remaining work is transcript normalization plus fixture sidecar loading, because benchmark/reporting and deterministic correctness checks now depend on a shared normalization contract before the benchmark command can land.
- item worked on: verification (transcript normalization and fixture sidecar loading) — added `src/parakeet/benchmark.py` helpers for spec-defined normalization, `.expected.txt` discovery, and normalized exact-match comparison, plus focused pytest coverage.
  key decisions: keep this iteration strictly to the shared normalization/sidecar contract so later benchmark work can build on one deterministic utility; do not pull the full `parakeet benchmark` CLI into the same change.
  files changed: `src/parakeet/benchmark.py`, `tests/test_normalization.py`, `.ralph/items.json`.
  verification commands run and outcomes: canonical repo verification commands were not defined beyond packaging metadata, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`21 passed`).
  notes for next fresh-context iteration: highest-priority remaining item is `parakeet benchmark --json`, because the normalization contract and sidecar loader now exist for deterministic fixture benchmarking and expected-transcript checks.
- item worked on: functional (`parakeet benchmark --json`) — added the packaged `benchmark` subcommand, real model load/transcribe helpers, and a deterministic silent WAV fixture plus benchmark pytest coverage for JSON arithmetic and expected-transcript sidecars.
  key decisions: keep this iteration strictly on the benchmark contract; use a local 16kHz silent fixture with an empty `.expected.txt` sidecar so the CLI smoke gate stays deterministic and offline; reuse one engine instance per invocation and report the final transcript plus normalized sidecar match.
  files changed: `src/parakeet/benchmark.py`, `src/parakeet/cli.py`, `src/parakeet/model.py`, `tests/test_benchmark.py`, `tests/fixtures/short_16k.wav`, `tests/fixtures/short_16k.expected.txt`, `.ralph/items.json`.
  verification commands run and outcomes: canonical verification commands were still not defined beyond packaging metadata, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pytest -q` ✅ (`24 passed`); `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/parakeet devices --json` ✅; `./.venv/bin/parakeet doctor --json` ✅; `./.venv/bin/parakeet doctor --check-model-cache --json` ✅; `./.venv/bin/parakeet benchmark --fixture tests/fixtures/short_16k.wav --runs 2 --json --check-expected` ✅ (`normalized_match=true`, exit 0; ALSA/JACK probe noise still appears on stderr for device/doctor commands and NeMo emits restore logs during benchmark load, but all commands succeeded without downloads).
  notes for next fresh-context iteration: highest-priority remaining item is the CI-safe verification suite/fixture coverage item, because benchmark fixtures now exist and the next milestone is closing the remaining contract-test gap without live microphone or network dependencies.
- item worked on: verification — tightened the CI-safe contract suite with offline test defaults, committed fixture coverage, and pytest metadata so config/doctor/devices/benchmark/normalization checks remain deterministic without microphone or network access.
  key decisions: keep this iteration strictly on the verification substrate; add `pytest` as a test extra plus `testpaths` metadata in `pyproject.toml`; force offline env vars in pytest and exercise the committed `tests/fixtures/short_16k.wav` benchmark path with mocked engine/transcribe hooks.
  files changed: `pyproject.toml`, `tests/conftest.py`, `tests/test_verification_suite.py`, `.ralph/items.json`.
  verification commands run and outcomes: canonical repo verification commands were still not defined beyond packaging metadata in `pyproject.toml`, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`25 passed`); `./.venv/bin/parakeet devices --json` ✅; `./.venv/bin/parakeet doctor --json` ✅; `./.venv/bin/parakeet doctor --check-model-cache --json` ✅; `./.venv/bin/parakeet benchmark --fixture tests/fixtures/short_16k.wav --runs 2 --json --check-expected` ✅ (timing/arithmetic/match succeeded, but smoke verification exposed NeMo info logs on stdout before the JSON payload, so the benchmark item was set back to failing with a regression note).
  notes for next fresh-context iteration: highest-priority remaining work is restoring a clean machine-readable `parakeet benchmark --json` contract by suppressing or redirecting NeMo stdout logging, then re-running the benchmark smoke gate before moving on to output adapters, VAD, or docs.
- item worked on: functional (`parakeet benchmark --json`) — restored the machine-readable benchmark contract by redirecting noisy runtime stdout away from JSON emission so stdout now contains exactly one JSON object for live smoke verification.
  key decisions: keep the iteration narrowly scoped to the benchmark regression; redirect third-party stdout to stderr only while engine load/transcribe runs, preserving human-visible logs without contaminating JSON stdout.
  files changed: `src/parakeet/benchmark.py`, `tests/test_benchmark.py`, `.ralph/items.json`.
  verification commands run and outcomes: canonical repo verification commands remain limited to `pyproject.toml` packaging/pytest metadata, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`26 passed`); `./.venv/bin/parakeet benchmark --fixture tests/fixtures/short_16k.wav --runs 2 --json --check-expected > /tmp/parakeet-benchmark.stdout.json 2> /tmp/parakeet-benchmark.stderr.log && ./.venv/bin/python - <<'PY' ... json.loads(...) ... PY` ✅ (`schema_version=1`, `normalized_match=True`, `len(run_ms)=2`, stdout parseable as a single JSON object while NeMo logs landed in stderr).
  notes for next fresh-context iteration: highest-priority remaining item is transcript output adapters (`text`/`json` stdout, optional file output, non-fatal clipboard failures), because the benchmark contract regression is closed and output behavior is the next remaining non-VAD functional surface before docs.
- item worked on: functional (transcript output adapters) — added `src/parakeet/output.py`, routed dictation status chatter away from JSON stdout, mirrored rendered transcript output to optional files, and kept clipboard failures non-fatal with debug-only warnings.
  key decisions: keep the iteration strictly on output behavior rather than VAD/docs; send interactive/status messages to stderr when `--format json` so stdout stays machine-readable; fix the latent `AppError` initialization bug because clipboard-failure handling now exercises it directly.
  files changed: `src/parakeet/output.py`, `src/parakeet/dictation.py`, `src/parakeet/errors.py`, `src/parakeet/types.py`, `tests/test_output.py`, `tests/conftest.py`, `tests/test_verification_suite.py`, `.ralph/items.json`.
  verification commands run and outcomes: canonical repo verification commands remain limited to `pyproject.toml` packaging/pytest metadata, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅; `./.venv/bin/python -m build` ✅; `./.venv/bin/python -m pytest -q` ✅ (`29 passed`); `./.venv/bin/python -m pytest -q tests/test_output.py` ✅ (`3 passed`); targeted automated dictation/output tests proved text + JSON rendering, `--output-file` mirroring, JSON stdout cleanliness, and non-fatal clipboard-failure behavior.
  notes for next fresh-context iteration: highest-priority remaining item is WebRTC-VAD auto-stop, because output behavior is now covered and VAD is the last major functional milestone before the README/documentation pass.
- item worked on: functional (WebRTC-VAD auto-stop) — added a WebRTC-backed VAD adapter plus a VAD recording path so dictation can auto-stop only after cumulative voiced audio reaches `min_speech_ms` and consecutive silence reaches `max_silence_ms`, while manual start/stop remains intact.
  key decisions: keep this iteration strictly on VAD runtime behavior; isolate the backend behind `build_vad_backend()`/`record_until_vad_stop()`; verify behavior with mocked frame sequences instead of live microphone input; add `webrtcvad-wheels` as the runtime dependency without changing README yet.
  files changed: `pyproject.toml`, `requirements.txt`, `src/parakeet/audio.py`, `src/parakeet/dictation.py`, `tests/test_vad.py`, `.ralph/items.json`.
  verification commands run and outcomes: canonical repo commands remain limited to `pyproject.toml` packaging/pytest metadata, so fallback gates were used — `./.venv/bin/python -V` ✅ (`Python 3.12.3`); `./.venv/bin/python -m compileall transcriber.py src tests` ✅; `./.venv/bin/python -m pip install -e .` ✅ (installed `webrtcvad-wheels`); `./.venv/bin/python -m build` ✅; `./.venv/bin/python -c 'from parakeet.audio import build_vad_backend; print(build_vad_backend(2).name)'` ✅ (`webrtc`); `./.venv/bin/python -m pytest -q tests/test_vad.py` ✅ (`4 passed`, covering voiced-audio eligibility, silence-triggered stop, no-speech manual fallback, and dictation VAD wiring); `./.venv/bin/python -m pytest -q` ✅ (`33 passed`); `./.venv/bin/parakeet devices --json` ✅; `./.venv/bin/parakeet doctor --json` ✅; `./.venv/bin/parakeet doctor --check-model-cache --json` ✅; `./.venv/bin/parakeet benchmark --fixture tests/fixtures/short_16k.wav --runs 2 --json --check-expected` ✅. ALSA/JACK probe noise still appeared on stderr for device/doctor commands and NeMo emitted benchmark logs to stderr, but all gates exited 0 and JSON stdout contracts stayed intact.
  notes for next fresh-context iteration: highest-priority remaining work is the README/documentation item, updating installation/usage/diagnostics/benchmark guidance to match the packaged CLI and now-documented VAD behavior with evidence-backed commands.
